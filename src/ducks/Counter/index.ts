import * as counterActions from './actions'
import * as counterTypes from './types'
import reducer, { initialState as counterInitialState } from './reducers'
export { counterTypes, counterActions, counterInitialState }

export default reducer
